# Cómo ejecutar la aplicación

1. Abrir una terminal en la carpeta principal `SACP-flask` y activar el entorno virtual ejecutando:

   ```bash
   source venv/bin/activate  # En Linux/MacOS
   venv\Scripts\activate     # En Windows
   ```

2. Iniciar el servidor Flask ejecutando el siguiente comando:

   ```bash
   python app.py
   ```
3. Entrar a la carpeta `sacp-front`:

   ```bash
   cd sacp-front
   ```

4. Iniciar la aplicación React ejecutando:

   ```bash
   npm start
   ```

5. Abrir un navegador web y navegar a `http://localhost:3000` para ver la aplicación en funcionamiento.